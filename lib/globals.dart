library sockapp.globals;

import 'package:camera/camera.dart';
import 'package:flutter/material.dart';

CameraDescription camera = CameraDescription(
    name: "empty",
    lensDirection: CameraLensDirection.back,
    sensorOrientation: 0);
